name: first-test

on: 
  push:
  pull_request:
    types: [closed, assigned, opened, reopened]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "test line"
      - name: shell command
        run: |
          df -h
          git --version
      - name: somename
        run: echo "another text line"
  
  deployment:
    runs-on: ubuntu-latest
      inputs:
        name:
          type: choice
          description: Who to greet
          options: 
          - monalisa
          - cschleiden
        message:
          required: true
        use-emoji:
          type: boolean
          description: Include 🎉🤣 emojis
    environment: ${{ github.event.inputs.message }}
    steps:
      - name: deploy
        run: echo "dummy"
