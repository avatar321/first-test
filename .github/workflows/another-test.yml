name: first-test

on: 
  repository_dispatch:
    types: [build]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "test line"
      - name: shell command
        run: |
          df -h
          git --version
      - name: somename
        run: echo "another text line"