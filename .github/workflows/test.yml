name: first-test

on: push

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "test line"
      - name: shell command
        run: echo "this is a test line to file" > /home/rootuser/softwares/first-test/testfile.txt
        run: |
          cpu -v
          git -v
          
